---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<div class:list={['font-mono text-[0.6875rem] text-accent tracking-[0.13em] uppercase', className]}>
  <slot />
</div>
